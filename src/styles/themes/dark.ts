import { DefaultTheme } from 'styled-components';

import { GREEN_BG_DARK, MAIN_BG_DARK } from '../constants';

const dark: DefaultTheme = {
  title: 'dark',
  colors: {
    greenBg: GREEN_BG_DARK,
    mainBg: MAIN_BG_DARK,
  }  
};

export default dark;
