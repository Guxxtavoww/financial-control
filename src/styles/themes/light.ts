import { DefaultTheme } from 'styled-components';

import { GREEN_BG_LIGHT, MAIN_BG_LIGHT } from '../constants';

const light: DefaultTheme = {
  title: 'light',
  colors: {
    greenBg: GREEN_BG_LIGHT,
    mainBg: MAIN_BG_LIGHT,
  },
};

export default light;
